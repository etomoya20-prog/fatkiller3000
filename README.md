# fatkiller3000

Телеграм-бот для группы худеющих: встречает новичков, считает индивидуальную норму
калорий, принимает ежедневные отчёты о еде в любом виде и раз в неделю публикует
в групповом чате сводку по всем участникам.

## Как это работает

**Новичок в группе.** Бот замечает вступление и пишет в чат приветствие с кнопкой,
ведущей в личку. Кнопка — диплинк, он же сообщает боту, из какой группы пришёл человек.

**Анкета в личке.** Шесть вопросов: пол, возраст, рост, текущий вес, целевой вес,
уровень активности. На выходе — дневная норма калорий и БЖУ.

Норма считается по формуле Миффлина-Сан Жеора: базовый обмен × коэффициент активности,
минус 20% на дефицит. Ниже базового обмена и ниже абсолютного минимума по полу
(1500 ккал мужчинам, 1200 женщинам) норма не опускается. Белок — 1.8 г на кг целевого
веса, жир — 0.9 г, углеводы — остаток.

**Ежедневный отчёт.** Пользователь присылает в личку что угодно:

- свободный текст — «завтрак овсянка 350, обед 600, ужин творог 250»;
- файл-выгрузку из приложения учёта калорий — CSV, XLSX, PDF, TXT;
- скриншот дневника питания;
- фотографию самой еды — тогда калорийность оценивается на глаз.

Оценку по фото бот записывает, но отдельным сообщением предупреждает, что это
прикидка с ошибкой до трети, советует завести трекер (FatSecret, MyFitnessPal,
YAZIO) и даёт ссылку на статью о дневнике питания. Ссылка меняется переменной
`GUIDE_URL`, пустое значение убирает её.

Всё это разбирает OpenAI и возвращает калории с БЖУ. Если пользователь прислал итог
за весь день, запись вытесняет всё накопленное за эту дату; если отдельный приём пищи —
прибавляется к дневной сумме.

**Распорядок дня** (время московское, меняется переменными в `.env`):

| Когда | Что |
|---|---|
| 19:00 ежедневно | Перекличка в группе: кто ещё не заполнил анкету |
| 21:00 ежедневно | Личные напоминания тем, кто не отчитался за день |
| 23:50 ежедневно | Обновление Google-таблицы |
| 20:00 в воскресенье | Итоги недели в группу |
| 10:00 в субботу | Личный вопрос про текущий вес |

Перекличка молчит, если анкеты заполнили все: ежедневное «все молодцы» перестают
читать через неделю. Недельная сводка молчит по той же причине, если за неделю
не отчитался вообще никто или считать пока не по кому: список из одних нулей —
это не итоги, а звать в анкету и без того каждый вечер ходит перекличка.

**Обнуление статистики.** Записи о еде можно стереть, не трогая анкеты, — группа
начинает считать заново. После этого в `.env` ставится `HISTORY_START` с датой
нового отсчёта: дни до неё выпадают из выгрузки и из сводки. Без этой границы
вся история до обнуления выглядит сплошным прогулом у всех, хотя записи оттуда
просто стёрты. Пустое значение снимает ограничение.

**Взвешивание.** По субботам в 10:00 бот пишет каждому в личку и просит цифру с
весов. Вопрос идёт с кнопкой: личка занята приёмом отчётов о еде, и без явного
подтверждения первый же «овсянка 350» уехал бы в весы. Если после кнопки пришло
не число, сообщение молча передаётся в разбор еды — взвешивание просто
пропускается.

Записанный вес становится текущим, а норма пересчитывается по той же формуле:
чем меньше вес, тем меньше тело тратит, и без пересчёта похудевший на десять
килограммов продолжал бы есть на старую норму. Вес, названный в анкете,
сохраняется отдельной колонкой `start_weight_kg` — по ней считается «сброшено
с начала». Взвеситься можно и вручную: `/weight` или сразу `/weight 82.5`,
история — `/weights`.

### Почему модель не считает сама

Перечислить раскладки всех приложений невозможно, поэтому надёжность строится иначе:
модель только **читает подписи**, а решение и арифметику делает код в `llm.py`.

Главная ловушка — крупная цифра на экране приложения означает, как правило, *остаток*
до нормы, а не съеденное. Модель раскладывает увиденное по отдельным полям
(`kcal_goal`, `kcal_remaining`, `kcal_consumed`), и `FoodReport._resolve()` выбирает,
чему верить. Когда подписанного «съедено» на экране нет, цель минус остаток
надёжнее крупной цифры: её слишком легко принять за съеденное.

Когда на экране есть и то, и другое, они регулярно расходятся — остаток приложение
считает от цели с учётом тренировок, а подписана рядом базовая цель. Тогда врёт
вычитание, а не подпись. Спор разбирает `_arbitrate()` третьим, независимым
числом — суммой по БЖУ: чья цифра к ней ближе, та и берётся. Если БЖУ не видно,
подписанному «съедено» верим, кроме одного случая: когда оно совпадает с остатком.
Это известный промах модели — одно число, положенное сразу в два поля.

Вторая опора — **сверка по БЖУ**. Белки, жиры и углеводы дают калорийность независимо
от того, как нарисован экран. Если сумма по ним расходится с прочитанным числом больше
чем на 25%, код берёт значение по БЖУ и пишет об этом человеку. Это ловит именно тот
случай, когда остаток приняли за съеденное.

Когда цифра не прочитана напрямую, а выведена, бот показывает откуда она взялась
и напоминает про `/reset`. Сырой ответ модели пишется в лог — по нему видно,
что именно она увидела, если разбор всё-таки промахнулся.

**Напоминание в 21:00 МСК.** Уходит тем, от кого за день не пришло ни одной записи.
Повторно за тот же день не приходит.

**Сводка по воскресеньям в 20:00 МСК.** В групповой чат уходит рейтинг участников:
сколько дней человек уложился в норму, сколько дней вообще не отчитался,
средние калории за неделю. День засчитан, если калории попали в коридор ±10% от нормы.

У тех, кто вступил среди недели, отсчёт идёт со дня прихода в группу, а не с
понедельника: иначе новичок в первой же сводке выглядел бы прогульщиком за дни,
когда его в группе не было. В строке такого участника стоит пометка
«с момента вступления», а знаменатель меньше семи. Значок «Идеальная неделя»
достаётся только тем, у кого за плечами полные семь дней без пропусков.

## Команды

| Команда | Где | Что делает |
|---|---|---|
| `/start` | личка | Анкета, а если она заполнена — показ нормы |
| `/today` | личка | Итог за сегодня |
| `/profile` | личка | Норма калорий и БЖУ |
| `/again` | личка | Заполнить анкету заново |
| `/reset` | личка | Стереть записи за сегодня |
| `/weight` | личка | Записать текущий вес (`/weight 82.5` — сразу) |
| `/weights` | личка | История взвешиваний |
| `/help` | личка | Как пользоваться |
| `/join` | группа | Записаться в участники вручную |
| `/chatid` | любой | ID чата (только админам) |
| `/force_summary` | любой | Разослать сводку сейчас (только админам) |
| `/force_reminder` | любой | Разослать напоминания сейчас (только админам) |
| `/force_nudge` | любой | Перекличка по анкетам сейчас (только админам) |
| `/force_weighin` | любой | Спросить про вес сейчас (только админам) |
| `/export` | любой | Обновить Google-таблицу сейчас (только админам) |

## Устройство

| Файл | Отвечает за |
|---|---|
| `main.py` | Запуск: БД, бот, планировщик, админские команды |
| `config.py` | Переменные окружения, коэффициенты активности |
| `calories.py` | Формула Миффлина-Сан Жеора, расчёт БЖУ |
| `db.py` | Пул asyncpg и все запросы |
| `schema.sql` | Таблицы, накатывается при каждом старте |
| `llm.py` | Разбор отчётов через OpenAI |
| `test_llm.py` | Проверка выбора итоговой цифры: `python test_llm.py` |
| `scheduler.py` | Напоминания, перекличка, взвешивание, выгрузка и сводка |
| `sheets.py` | Ежедневная выгрузка в Google Sheets |
| `handlers/group.py` | Приветствие новичков, отслеживание состава группы |
| `handlers/onboarding.py` | Анкета на конечном автомате |
| `handlers/intake.py` | Приём текста, файлов и картинок |
| `handlers/weighin.py` | Субботнее взвешивание и история веса |

Данные лежат в PostgreSQL **на хосте**, не в контейнере — на сервере мало памяти.
Контейнер ходит в базу через `host.docker.internal`.

## Развёртывание

Сервер: `root@150.251.155.201`, каталог `/srv/fatkiller3000`.
База `fatkiller3000` и пользователь `fatkiller` уже созданы, пароль прописан
в `/srv/fatkiller3000/.env`.

Перед первым запуском заполните в `/srv/fatkiller3000/.env`:

```
BOT_TOKEN=          # от @BotFather
OPENAI_API_KEY=     # ключ OpenAI
ADMIN_IDS=          # ваш Telegram ID, через запятую если несколько
GROUP_CHAT_ID=      # можно оставить пустым
```

Дальше:

```bash
./deploy.sh
```

Скрипт коммитит изменения, пушит в GitHub, на сервере делает `git reset --hard`,
пересобирает образ и поднимает контейнер.

Полезное на сервере:

```bash
docker compose -f /srv/fatkiller3000/docker-compose.yml logs -f    # логи
docker compose -f /srv/fatkiller3000/docker-compose.yml restart    # перезапуск
```

## Настройка бота в Telegram

1. Создайте бота у @BotFather, токен — в `.env`.
2. Отключите Privacy Mode: `/setprivacy` → Disable. Иначе бот не увидит команды в группе.
3. Добавьте бота в группу и **сделайте администратором** — без этого Telegram не
   присылает события о новых участниках, и встречать новичков бот не сможет.
4. Проверьте: команда `/chatid` в группе, затем `/force_summary` в личке.

### Те, кто был в группе до бота

Telegram не отдаёт список участников и не рассказывает задним числом, кто вступил
раньше. Поэтому старожилов бот узнаёт по их сообщениям: любое сообщение в чате тихо
записывает автора в состав группы, без ответа. Достаточно один раз что-нибудь написать.
Кто в чате не пишет — может позвать `/join`.

Пока человек не привязан ни к одной группе, он не попадает в сводку и не получает
напоминаний. Бот предупреждает об этом сам — в конце анкеты и в `/profile`.

### Администратор как участник

`ADMIN_IDS` открывает только три служебные команды и на участие никак не влияет.
Администратор заполняет анкету, отчитывается о еде, получает напоминания и попадает
в сводку наравне со всеми.

## Несколько групп

Бот рассчитан на работу сразу в нескольких группах — отдельный экземпляр под каждую
не нужен.

Профиль и дневник питания привязаны к человеку, а не к чату: у таблиц `users`
и `entries` нет `chat_id`. Человек, состоящий в двух группах, заполняет анкету один
раз, отчитывается один раз в день и получает одно напоминание — но попадает в сводки
обеих групп с одинаковыми цифрами.

К чату привязана только принадлежность: `group_members` — связка «многие ко многим»,
и сводка строится по составу конкретной группы. Участники группы A в сводке группы B
не появляются.

Что происходит на краях:

- **Вышел из одной группы из двух** — остаётся в сводке второй, напоминания приходят.
- **Вышел отовсюду** — напоминания прекращаются, данные сохраняются. Вернётся —
  анкету заполнять заново не придётся.
- **Бота выгнали из группы** — чат помечается неактивным, сводка туда не уходит,
  участники этой группы перестают получать напоминания, если других групп у них нет.

Оставьте `GROUP_CHAT_ID` пустым — тогда сводка автоматически уходит во все группы,
где состоит бот. Заполнять его нужно, только если сводку надо публиковать не везде;
несколько ID перечисляются через запятую.

Учтите: человек в двух группах показывает свои результаты обеим.

## Google-таблица

Каждую ночь в 23:50 бот перезаливает таблицу целиком — тремя листами:

| Лист | Что внутри |
|---|---|
| `Участники` | Анкеты и нормы, по строке на человека. Там же старт, текущий вес и сколько сброшено. Незаполнившие тоже здесь, со статусом «нет анкеты» |
| `Дневник` | По строке на каждый день каждого участника: калории, БЖУ, процент от нормы, попал ли в коридор |
| `По дням` | Сводная матрица «участник × дата» с калориями в ячейках |

Лист переписывается, а не дополняется. Это важно: отчёт, присланный после 23:50,
попадёт в таблицу следующей ночью, а не потеряется. Побочный плюс — `/export`
можно жать сколько угодно раз подряд.

Дни без отчёта остаются **пустыми**, а не нулевыми: ноль калорий и отсутствие
отчёта — разные вещи, и если их смешать, поедут все средние.

На листе `По дням` колонки общие для всех, поэтому у новичка слева оказываются
дни, когда его в проекте ещё не было. Прогул помечается прочерком, а «не
участвовал» остаётся пустой ячейкой — иначе вступивший вчера выглядит так,
будто месяц молчал.

### Как подключить

1. В Google Cloud завести сервисный аккаунт и включить ему **Google Sheets API**,
   скачать ключ в формате JSON.
2. Положить ключ на сервер в `/srv/fatkiller3000/secrets/google-service-account.json`.
   Каталог `secrets/` под git не ходит и переживает деплой — как и `.env`.
3. Создать таблицу и **расшарить её на email сервисного аккаунта** (вида
   `имя@проект.iam.gserviceaccount.com`) с правом редактирования. Без этого шага
   аккаунт таблицу просто не увидит.
4. В `.env` прописать `GOOGLE_SHEET_ID` — это часть ссылки между `/d/` и `/edit`.
5. Задеплоить и проверить командой `/export`.

Пустой `GOOGLE_SHEET_ID` выключает выгрузку целиком: задача в планировщик не
попадает, бот работает как раньше.

## Что стоит знать

Анкета живёт в памяти процесса. Если контейнер перезапустится, пока человек отвечает
на вопросы, ему придётся начать с `/start` — данные уже отвеченных вопросов не сохранятся.
Для шести коротких вопросов это приемлемо; если станет мешать, состояние переносится в БД.

Разбор отчётов стоит денег: каждое сообщение о еде — один запрос к OpenAI.
На `gpt-4o-mini` это копейки, но при росте группы имеет смысл посматривать на расход.

Участники группы видят в еженедельной сводке имена и результаты друг друга.
Это задумано, но людей стоит предупредить при входе в группу.
